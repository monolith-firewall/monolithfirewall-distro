# Build Directory

This directory contains build artifacts and outputs.

## Structure

- `bin/` - Compiled binaries and executables
- `obj/` - Intermediate object files
- `release/` - Release builds ready for distribution
